on:
  workflow_call:
    inputs:
      run-tests:
        required: false
        type: boolean
        default: true

jobs:
  tests:
    runs-on: macos-latest
    if: ${{ inputs.run-tests }}

    steps:
      - uses: actions/checkout@v3

      - name: Setup Flutter
        uses: ./.github/actions/setup-flutter

      - name: Run tests
        run: flutter test --reporter expanded

  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Flutter
        uses: ./.github/actions/setup-flutter

      - name: Flutter Doctor
        run: flutter doctor -v

      - name: Build binary
        run: flutter build macos

      - name: Upload binary
        uses: actions/upload-artifact@v3
        with:
          name: macos-dev
          path: build/macos/Build/Products/Release/open_edisu.app
          if-no-files-found: error
